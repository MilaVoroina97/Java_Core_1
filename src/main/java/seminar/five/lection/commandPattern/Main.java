package seminar.five.lection.commandPattern;

public class Main {

/*app.add(() -> speaker.play());
app.add(() -> speaker.volumeUp());
app.add(() -> speaker.volumeUp());
app.add(() -> speaker.volumeDown());
app.add(() -> speaker.volumeDown());
app.add(() -> speaker.pause());


Moreover, we could simplify the code even more by using method references:

app.add(speaker::play);
app.add(speaker::volumeUp);
app.add(speaker::volumeUp);
app.add(speaker::volumeDown);
app.add(speaker::volumeDown);
app.add(speaker::pause);*/
}
